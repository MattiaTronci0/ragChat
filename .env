# AnythingLLM Configuration
# Update these values after setting up AnythingLLM

# Required: JWT secret for AnythingLLM (change this in production)
JWT_SECRET=ragchat-jwt-secret-key-change-this-in-production-for-security

# Required: AnythingLLM API key (generate this after first setup - server-side only)
ANYTHINGLLM_API_KEY=your-api-key-will-be-generated-after-first-setup

# Required: Default workspace name
ANYTHINGLLM_WORKSPACE=default-workspace

# Optional: LLM Provider Configuration
LLM_PROVIDER=ollama
EMBEDDING_ENGINE=ollama
VECTOR_DB=lancedb

# Development Configuration
NODE_ENV=development